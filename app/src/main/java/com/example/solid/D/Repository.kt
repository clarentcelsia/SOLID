package com.example.solid.D

//how program works
class Repository {

    fun adding(x: Int, y: Int): Int{
        return x + y
    }

    fun subtracting(x: Int, y: Int): Int{
        return x - y
    }
}